name: gnn
channels:
  # These do NOT commute!
  - pytorch
  - ostrokach-forge
  - conda-forge
  - scikit-learn
dependencies:
  - python >= 3.10
  - pytorch >= 2
  - optuna
  - wandb
  - numpy < 2
  - pandas
  - scikit-learn
  - pip
  - jupyter
